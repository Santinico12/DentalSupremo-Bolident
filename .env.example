# ==========================================================
# CONFIGURACIÓN DEL SISTEMA - DRA. TATIANA RUIZ (PHP BACKEND)
# ==========================================================

# 1. Base de datos MySQL de tu hosting
DB_HOST=localhost
DB_NAME=nombre_de_tu_base_de_datos
DB_USER=usuario_mysql
DB_PASS=contrasena_mysql
DB_CHARSET=utf8mb4

# 2. Clave secreta para comunicación segura con el Bot de WhatsApp
# (IMPORTANTE: Debe ser EXACTAMENTE la misma que en el microservicio del bot)
API_BRIDGE_KEY=tatianaruiz_secret_bridge_key_2026

# 3. URL pública del microservicio del bot (donde esté desplegado: Render, Railway, VPS, etc.)
# Ejemplo: https://dratatianaruiz-bot.onrender.com/api
BOT_API_URL=https://dratatianaruiz-bot.onrender.com/api
